import SwiftUI

class ScreenshotViewModel: ObservableObject {
//    let service = ScreenshotService()
//    let captureService = ScreenCaptureService()
//    
    func sendScreenShot() {
//        guard let image = captureService.captureUIImage() else { return }
//        Task {
//            //        guard let image = await screenshotManager.capture() else {
//            //            
//            //        }
//            await service.sendScreenshot(image: image)
//        }
    }
}
